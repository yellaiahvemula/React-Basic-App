import React from "react";

export default class Ttile extends React.Component {
    render(){
        return(
            <div>
                <h1>{this.props.title}</h1>               
            </div>            
        );
    }
    
}
